$Id$


# ksmutil usage

## global options

	--config <config>

## commands

	setup
	update

	zone add
		--zone <zone>
		[--policy <policy>]
		[--signerconf <signerconf.xml>]
		[--input <input>]
		[--output <output>]
	zone delete
		--zone <zone> | --all
	zone list

	repository list

	policy export --policy <policy>
	policy list

	key list
		[--verbose]
		--zone <zone> | --all
		[--keystate <state>]
		[--keytype <type>]
		[--ds]

	key export
		--zone <zone> | --all
		[--keystate state]
		[--keytype keytype]
		[--ds]

	key import
		--cka_id <CKA_ID>
		--repository <repository>
		--zone <zone>
		--size <size>
		--algorithm <algorithm>
		--keytype <type>
		--keystate <state>
		--time <time>
		[--retire <retire>]
		
	key rollover
		[--zone zone]
		[--policy policy]
	
	key purge --zone <zone>
	key purge --policy <policy>

	backup done --repository <repository>
	backup list --repository <repository>
	
	rollover list


key states: GENERATED|PUBLISHED|READY|ACTIVE|RETIRED|REVOKED|DEAD
key types:  KSK|ZSK
